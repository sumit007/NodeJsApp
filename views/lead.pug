doctype html
html(lang='en')
	include common/head.pug
	+head('MVP starter project')
	body
		h1 Lead
		p 
			span #{lead.email}

			a(href="/lead/" + lead.id + "/edit") Edit
			
			form(action='/lead/' + lead.id + "/delete", method="POST")
				button(type="submit") Delete

		include common/footer.pug
		+footer()